---
'markdown-to-jsx': minor
---

Add Vue.js 3+ integration. Includes `compiler`, `parser`, `astToJSX`, and `Markdown` component. Vue uses standard HTML attributes (class, not className) with minimal attribute mapping (only 'for' -> 'htmlFor').
