---
'markdown-to-jsx': patch
---

Fix `<hr>` and other void HTML elements silently dropping all subsequent content when not followed by a newline (#856)

修复 `<hr>` 及其他空元素在未跟随换行符时静默丢弃所有后续内容的问题（#856）

`<hr>` और अन्य void HTML तत्वों द्वारा नई पंक्ति न होने पर बाद की सभी सामग्री को चुपचाप हटाने की समस्या ठीक की गई (#856)
