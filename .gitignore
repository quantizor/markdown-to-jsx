node_modules/
.git/
.DS_Store
coverage/
*.cpuprofile
CPU*.md
*.log
# non-bun package-manager artifacts (this repo uses bun; bun.lock is authoritative)
package-lock.json
pnpm-lock.yaml
pnpm-workspace.yaml
yarn.lock
*.tsbuildinfo

# ephemeral files
dist/
.cursor/plans

# Per-machine dev server registrations (devctl); each machine keeps its own.
devservers.json

# Harness artifacts (Playwright, Expo)
test-results/
playwright-report/
blob-report/
.expo/
