Can you make your changes more concise? Try to achieve net-negative LOC through logical consolidation and code reuse. Do not create helpers unless there are at least two instances of the same pattern.

Be sure to test your changes with `bun run test` and `bun metrics` (run 3x).
